require('dotenv').config(); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const order = await prisma.order.findFirst({ orderBy: { id: 'desc' } }); const user = await prisma.customer.findFirst(); console.log('Updating order', order.id, 'to customer', user.id); await prisma.order.update({ where: { id: order.id }, data: { customerId: user.id } }); console.log('Done!'); } main();  
